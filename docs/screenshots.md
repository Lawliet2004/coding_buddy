# Screenshots and Demo Media

The current demo asset is a lightweight generated GIF:

![tokenmaxxing-ai demo](assets/tokenmaxxing-ai-demo.gif)

It shows the expected terminal flow:

1. Install generated agent workflows with `npx tokenmaxxing-ai install --yes --verify`.
2. Invoke `$simplify` to get a plan, approval gate, edit, and verification loop.
3. Invoke `$review-ultra` to run a structured whole-repository review with findings and a fix plan.

After the npm package is published, replace or supplement this generated asset with a captured terminal recording from a clean install.
