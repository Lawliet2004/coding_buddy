interface:
  display_name: "Review Lite"
  short_description: "Fast review with adaptive project memory"
  brand_color: "#2563EB"
  default_prompt: "Use $review-lite to run a fast approval-first review. Update .tokenmaxxing.md when done."

policy:
  allow_implicit_invocation: true
