<!-- tokenmaxxing-ai:generated -->
# Tokenmaxxing Project Memory

## Project Profile
- Type: Node CLI installer (npm package, zero runtime deps, ESM only, Node ≥20)
- Primary languages: JavaScript
- Verification commands: `npm test`, `npm run check`, `npm.cmd --cache .npm-cache pack --dry-run`, installer smoke tests for dry-run/project/user scopes
- High-risk areas: `src/install.js` (path resolution, file writes, block-merge regex), `src/cli.js` (arg parsing, injected cwd/env path resolution, approval prompt)
- User preferences: aggressive test coverage; high simplification tolerance; full security checklist (N/A items documented)
- Recurring false positives: skill reference files (`security_checklist.md`, `bug_patterns.md`, `maintainability_checklist.md`, `simplification_signals.md`) are byte-identical copies across `skills/review-{lite,mid,ultra}/references/` — intentional for per-skill standalone packaging, do not consolidate

## Command Run Counts
- /simplify: 0
- /review lite: 0
- /review mid: 0
- /review ultra: 2

## Adaptive Instructions
- Prefer: behavior-preserving path-display fixes; export internal helpers (`materialize`, `safeResolve`, `escapeRegExp`, `addTargets`, `splitList`) directly from their owning module for unit-test coverage rather than adding new utility files
- Avoid: refactoring `skills/review-*/references/*` (intentional duplication for standalone skill packaging); introducing runtime dependencies (the package must stay zero-dep); committing local analysis output such as `graphify-out/`
- Check first: any change to `src/install.js` does not break `install --verify` for block-merge files; `safeResolve` continues to reject `..` traversal and absolute paths; `runCli` resolves relative `--dir` and injected home env against injected test context

## Recent Runs
| Date | Command | Scope | Outcome | Verification |
| --- | --- | --- | --- | --- |
| 2026-06-16 | /review ultra | whole repo including `.agents/` and `graphify-out/` | 0 critical, 0 high, 3 medium fixed (relative `--dir`, injected home env, ignored graph output), 2 low fixed (CI permissions, CLI/install cycle), 2 new CLI regression tests | npm test 57/57, npm run check, npm pack --dry-run, install --dry-run, install --yes --verify (project + user), git diff --check |
| 2026-06-16 | /review ultra | whole repo minus .agents/ | 0 critical, 0 high, 6 medium, 11 low findings; 1 medium fixed (F-1 home-path leak), 1 low fixed (F-7 dead 'skipped' branch), 4 simplifications (F-4/F-5/F-8/F-9), 32 new tests | npm test 55/55, npm run check, install --dry-run, install --yes --verify (project + user) |
